rew run(
  (
    (entity "example_and" is
      port (("A" : in  bit ;)  ("B" : inout  'std_logic ;) ("C" : inout  'std_logic ;)  ) ;
    end "example_and" ) ;
    (architecture "behaviour" of "example_and" is nop ;
      begin (
            ("B" <= (stdlogicVal('0') or "A")) ;
            ("C" <= (stdlogicVal('0') or "B")) ;
      )
    end "behaviour" );
  ), empty) .